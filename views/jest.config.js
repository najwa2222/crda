module.exports = {
  testMatch: ['<rootDir>/__tests__/**/*.test.js'], // Chercher les fichiers de test avec l'extension .test.js dans le répertoire __tests__
  testPathIgnorePatterns: ['<rootDir>/node_modules/'], // Ignorer les fichiers dans le répertoire node_modules
};

