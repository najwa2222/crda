pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                git branch: 'master', url: 'https://github.com/najwa2222/crda.git', credentialsId: 'git-credentials'
            }
        }
        
        stage('Build') {
            steps {
                bat 'npm install' // Installer les dépendances
                
            }
        }
        
        stage('Unit Tests') {
            steps {
                bat 'npm test' // Exécuter les tests unitaires
            }
        }
        
        stage('Integration Tests') {
            steps {
                // Exécuter les tests d'intégration
                bat 'npm run test:integration' // Assurez-vous que ce script est défini dans package.json
            }
        }
        
        stage('Security Tests') {
            steps {
                // Exécuter les tests de sécurité
                bat 'npm run test:security' // Assurez-vous que ce script est défini dans package.json
            }
        }
        
        stage('Build Docker Image') {
            steps {
                bat 'docker build -t najwakarrouchi968/auth-service:latest .' // Construire l'image Docker
            }
        }
        
        stage('Push Docker Image') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'dockerhub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
                    bat "echo %PASSWORD% | docker login -u %USERNAME% --password-stdin"
                    bat "docker push najwakarrouchi968/auth-service:latest" // Pousser l'image Docker vers Docker Hub
                }
            }
        }
        
        stage('Deploy') {
            steps {
                script {
                    kubernetesDeploy(
                        configs: 'k8s/auth-deployment.yaml',
                        kubeconfigId: 'kubeconfig'
                    )
                }
            }
        }
    }
}
